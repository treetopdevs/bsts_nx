# Implementation Plans

Generated by the `improve` skill. Two audit passes so far:

- **2026-06-20** (standard) → plans **001–006**, all **DONE** (applied in the working
  tree; see note below).
- **2026-06-30** (deep) → plans **007–014**, **TODO**.

Both audited against commit `e4654c5`. Execute in the order below unless dependencies
say otherwise. Each executor: read the plan fully before starting, honor its STOP
conditions, and update your row in the table when done.

> **Working-tree note**: plans 001–006 were executed but their changes are **not yet
> committed** (the `lib/` edits and new `test/*.exs` files sit uncommitted on top of
> `e4654c5`). So the live working tree already contains those fixes. Every plan below
> relies on **comparing its "Current state" excerpts against the live code** rather
> than trusting the SHA-based drift check alone — do that.

> **Tooling note for all plans**: `mix` on this machine is a `mise` shim. Plain
> `mix <cmd>` works in CI and usually locally. If a local `mix` invocation errors
> with `unexpected argument '-n'` or a `mise exec` usage message, prefix the command
> with `mise exec -- ` (e.g. `mise exec -- mix test <file>`). `.tool-versions` pins
> Erlang 28.3.1 / Elixir 1.19.5-otp-28. Source-of-truth verifier for the whole repo:
> `bash scripts/ci.sh` (compile `--warnings-as-errors`, non-external tests, format
> check, docs build).

## Execution order & status

### Batch 1 — 2026-06-20 (standard audit)

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 001 | Fix missing-observation initial-state at the two wrapper sites the 5035d69 sweep missed | P1 | S | LOW | — | DONE |
| 002 | Add correctness tests for missing-observation handling (compiled filter + structured Gibbs) | P2 | M | LOW | — | DONE |
| 003 | Stop publishing internal code-review docs in ExDoc/Hex | P2 | S | LOW | — | DONE |
| 006 | Reject ambiguous-length rank-2 observation matrices explicitly | P3 | S | LOW | — | DONE |
| 005 | Untrack the stale `optimize-plan.md` planning artifact and remove the crash dump | P3 | S | LOW | — | DONE |
| 004 | Deduplicate the scalar RTS smoother backward pass | P3 | M | MED | 002 | DONE |

### Batch 2 — 2026-06-30 (deep audit)

| Plan | Title | Priority | Effort | Risk | Depends on | Status |
|------|-------|----------|--------|------|------------|--------|
| 007 | Stop `.formatter.exs` from importing optional/dev-only deps (`:exla`, `:ex_doc`) | P2 | S | LOW | — | TODO |
| 008 | Add a CI-runnable correctness test for `CausalImpact.estimate/4` | P2 | M | LOW | — | TODO |
| 009 | Consolidate the duplicated positive-integer validation into `Utils` | P3 | S | LOW | — | TODO |
| 010 | Add a scalar-vs-multi agreement test for the compiled Kalman filter | P3 | S | LOW | — | TODO |
| 011 | Align the EXLA CI job's compile flags with the main test job | P3 | S | MED | — | TODO |
| 012 | Document the undocumented `BSTS_NX_RSCRIPT` and benchmark env vars | P3 | S | LOW | — | TODO |
| 013 | Clamp the percentile slice indices in `SpotAttributor.attribute_posterior` | P3 | S | LOW | — | TODO |
| 014 | (SPIKE) Design a public backcast / missing-data imputation API | P3 | M | LOW | — | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (one-line reason) | REJECTED (one-line rationale)

## Recommended sequencing (Batch 2)

All eight are independent (no inter-plan dependencies). Suggested order by leverage and risk:

1. **007** first — clean, mechanical, removes contributor `mix format` friction; zero behavior change.
2. **008** — highest value: makes the flagship `CausalImpact.estimate/4` verifiable in the default CI gate (its only tests are currently `:external`/skipped). Test-only, zero source risk.
3. **009** — safe, message-preserving validation dedup. Note it deliberately scopes
   *out* the riskier period-validator merge (see rejected/deferred below).
4. **010**, **013** — small, low-risk test/defensive-fix polish; any order.
5. **012** — docs-only; any time.
6. **011** — do when an EXLA-capable environment is available (it needs a local EXLA
   compile to decide whether to add `--warnings-as-errors`; has an escape hatch if EXLA
   can't be built locally).
7. **014** — a design SPIKE; produces a recommendation + `@moduledoc false` prototype,
   not a shipped feature. Run when evaluating the backcast direction.

## Dependency notes

- Batch 1: **004 requires 002** (002's missing-data correctness tests guard 004's
  smoother refactor). All Batch 1 plans are DONE.
- Batch 2: no inter-plan dependencies.

## Findings considered and rejected (do not re-audit)

### From the 2026-06-20 pass

- **Gamma-sampler NaN "corruption"** (`distributions.ex` ~287-295): by-design fail-loud
  sentinel for Marsaglia-Tsang rejection-exhaustion; the proposed "floor to 1e-300"
  would reintroduce a silent point-mass the author deliberately rejected.
- **Smoother "eager-vs-defn covariance clamp mismatch"** (`smoother.ex` ~759 vs ~772):
  misread — both clamp *different* quantities in the same eager path.
- **R sidecar command injection** (`r_sidecar.ex`): well-defended (list-arg `Port.open`,
  isolated env, `:exclusive` 0600/0700 temp files, prefix-validated cleanup).
- **Nx 0.12 dependency lag**: current and version-aligned; no action.
- **Relative-effect divide-by-zero**: already guarded (`validation.ex`, `@relative_effect_epsilon`).
- **No Dialyzer/Credo**: real DX gap but MED-effort with Nx false-positive noise; low
  leverage for a library this size.
- **Performance micro-items** (`rolling_baseline.ex:345`, `forward.ex:413`,
  `causal_impact.ex:331`): off the hot path, ~ms-scale; not worth the refactor risk.

### From the 2026-06-30 deep pass

- **Core numerical correctness & security**: thorough passes found **no new bugs**.
  The engine is well-defended (guarded `sqrt`/division, consistent NaN missing-data
  handling across eager/defn, verified backward-smoother indexing + lag-1 cross-cov,
  Cholesky jitter/eigenvalue fallbacks). The R-sidecar re-review confirmed the prior
  "well-defended" verdict (static R template, values via env vars — no R-code injection).
- **PERF (residuals host roundtrip)** (`gibbs_sampler/residuals.ex:19,31`): the
  `Nx.to_number` extracts a scalar that the host-side inverse-gamma variance sampler
  genuinely needs; near-free under the default BinaryBackend; on the explicitly-slow
  eager-MCMC path. Not a win.
- **PERF (spike_slab cluster)** (`gibbs_sampler/spike_slab.ex` — `List.replace_at` O(p²)
  gamma loop, nested-`Enum` covariance build, eager rank-1 updates, tuple-matrix XtX):
  real patterns, but dominated by the per-column g-prior matrix algebra in the same
  loop, in a module **deliberately optimized in `da81702`** ("pre-computed XtX/Xty"),
  and the rank-1 rewrite is HIGH-risk. Needs benchmarking to justify; not worth the
  risk now.
- **DEBT (two percentile methods)** (`causal_impact.ex:1065 linear_percentile` vs
  `utils.ex:387 percentile_interval`): **intentional** — linear interpolation for MCMC
  posteriors vs nearest-rank elsewhere. Consolidating changes statistical output; not a
  mechanical dedup.
- **DEBT (split the `CausalImpact` god module, 1102 lines)**: high effort, MED risk,
  debatable leverage for a library this size, and risky for a handoff executor.
- **DEBT (period-validator unification)** — `operational.ex:511`, `model_builder.ex:497`,
  `r_sidecar.ex:445`: **deferred, not rejected.** They return different shapes and
  `operational`'s omits the obs-length bound that `Validation.validate_study_periods!`
  enforces, so merging risks behavior change in the numerical index path. Plan 009
  explicitly scopes this out; it warrants its own plan with parity tests if pursued.
- **DEBT (non-empty validation dedup)** — deferred within plan 009: the messages differ
  across sites, so it needs message standardization + test updates; pull into its own
  follow-up.
- **DEBT (layering / spec-building scatter / Validation dead-code trace / Applications
  option-parsing)**: fuzzy/architectural, need judgment a handoff executor can't supply
  well; revisit only if they become pain points.
- **TEST (defn-vs-eager agreement for inv-gamma sampling)**: low added value — both the
  defn and eager inv-gamma samplers are already validated against the *analytical*
  inverse-gamma distribution independently (`distributions_defn_gamma_test.exs`,
  `distributions_inv_gamma_test.exs`), which is stronger than comparing them to each
  other.

## Direction options (maintainer's call)

- **PLANNED as 014** — **Public backcast/imputation API** (prior D2 / DIR-02): the
  smoother already fills masked gaps inside `estimate_from_filter/3`; no public way to
  impute user gaps. Now a design SPIKE (plan 014).
- **D1 / DIR-01 & DIR-05 — Multivariate observations**: README roadmap promises "full
  multivariate-observation support" while structured MCMC is scalar-obs-only
  (`gibbs_sampler.ex:52,328`, `operational.ex:374`). The compiled filter handles
  multidim *state* but the MCMC path enforces scalar *observations* — a hard asymmetry.
  Large (L–XL); aligns with the stated roadmap. Either tighten the README wording or
  open a design spike.
- **D3 / DIR-03 — Public "fast baseline from filter" API**: `estimate_from_filter/3`
  works but takes raw matrices; `Operational` is tied to spot attribution. A friendly
  counterfactual-without-MCMC entry point is one wrapper away (S spike).
- **DIR-04 — AR + holiday component families**: `bct/ar_forecaster.ex` exists but is a
  separate forecaster, not composable into `ModelSpec`; holiday/event components are
  absent (users hand-roll them as regressors). M–L per component.
- **DIR-06 — Position `Operational` as a first-class inference strategy in docs**: the
  module is mature and public but the README "Status" doesn't list it alongside Gibbs
  and CausalImpact, nor say when to choose it (S, docs).
